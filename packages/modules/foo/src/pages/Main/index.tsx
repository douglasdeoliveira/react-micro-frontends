import React from 'react';

import { ComponentA } from '@shared/components';

const Main: React.FC = () => (
  <div className="container">
    <h1>Foo module</h1>
    <ComponentA />
  </div>
);

export default Main;
